Memorialize prompts into `./_docs`.

Write all my prompts and your responses from this session into a markdown file.

If `/dump` was used earlier in this session, don't include anything prior to
that dump. If `/clear` was used, don't include anything prior to that clear.

Put this markdown file in a `./_docs` directory at the root of this project,
which is most likely adjacent to a `/.git` directory. The filename should match
the format `YYYY-MM-DD-<count>-<phrase>`, where `<count>` increments by one for
each new file in the `/_docs` dir (starting at 1), and `<phrase>` is a short
phrase describing our conversation so far.

Also, $ARGUMENTS. Thanks!
